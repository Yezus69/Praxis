Deliverable summary:

1. Changed [praxis/train.py](C:\Users\Asav\source\repos\Praxis\praxis\train.py:56):
   - defaults added at lines 56-66
   - `train_metrics.csv` eval-branch logging added at lines 211-317
   - argparse flags added at lines 381-403
   - config resolution added at lines 441-451
   - guarded PPO kwarg injection added at lines 584-642

2. AST syntax check passed:
   - `AST OK`

3. Default no-flag invariant confirmed:
   - `--lr-schedule` defaults to `none`
   - nullable knobs default to `None`
   - `--deterministic-eval` defaults to `False`
   - therefore all new Step-4 blocks are skipped and inject zero new `train_kwargs` on a no-flag run.