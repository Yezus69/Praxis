# PMA-C full-ALE-Atari continual PPO (deployed certified-champion routing).
# Python >= 3.11, Linux / WSL2 + CUDA (JAX has no native-Windows CUDA wheel).
#
# Install order:
#   pip install -r requirements.txt
#   pip install -U "jax[cuda12]==0.9.2"        # LAST, so the CUDA jaxlib wins
#   bash pmac/envs/setup_envpool.sh            # real ALE via envpool + ALE ROMs (installed --no-deps)

numpy
flax
optax
matplotlib
pytest

# jax[cuda12]==0.9.2   # install separately, last (see above)
# envpool              # installed by pmac/envs/setup_envpool.sh
